query Menu($first: IntType = 100, $skip: IntType = 0) {
	allCollections(first: $first, skip: $skip) {
		id
		title
		image {
			...ImageThumnbailFragment
		}
		slug
	}
	_allCollectionsMeta {
		count
	}

	allAbouts(first: $first, skip: $skip) {
		id
		title
		slug
		image {
			...ImageThumnbailFragment
		}
	}
	_allAboutsMeta {
		count
	}

	allFaqSections(first: $first, skip: $skip) {
		id
		title
		slug
		image {
			...ImageThumnbailFragment
		}
	}
	_allFaqSectionsMeta {
		count
	}

	general {
		instagram
		facebook
		twitter
	}
}
